((core) => {
    /*var Collection = core.BaseClass.extend({
        initialize() {
            var args = [].slice.call(arguments);

            core.each(args, (item, i) => {
                this[i] = item;
            });
        }
    });

    var fn = Collection.prototype.constructor = [];*/

    var Coll3ection = Array;

})(window.Grim);